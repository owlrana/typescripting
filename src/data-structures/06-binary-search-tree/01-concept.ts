/**
 * ============================================
 * Binary Search Tree – Concept & Basics
 * ============================================
 *
 * A Binary Search Tree (BST) keeps keys in an ordered tree so that lookup, insertion, and deletion each take O(h) where h is the tree’s height.
 *
 * Big‑O complexity (typical):
 * – Access:    depends
 * – Insert:    depends
 * – Delete:    depends
 *
 * Below is a minimal illustrative implementation.
 */
export class BinarySearchTreeConcept<T> {
    private data: T[] = [];
    size(): number { return this.data.length; }
    // Add your own basic methods…
}
