/**
 * Implementation of Binary Search.
 */
export function binarysearch(arr: number[]): number[] {
    const a = [...arr];
    // TODO implement binary-search
    return a;
}

if (require.main === module) {
    console.log(binarysearch([5,2,4,3]));
}
