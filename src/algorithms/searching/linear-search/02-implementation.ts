/**
 * Implementation of Linear Search.
 */
export function linearsearch(arr: number[]): number[] {
    const a = [...arr];
    // TODO implement linear-search
    return a;
}

if (require.main === module) {
    console.log(linearsearch([5,2,4,3]));
}
