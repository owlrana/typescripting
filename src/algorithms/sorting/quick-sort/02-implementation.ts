/**
 * Implementation of Quick Sort.
 */
export function quicksort(arr: number[]): number[] {
    const a = [...arr];
    // TODO implement quick-sort
    return a;
}

if (require.main === module) {
    console.log(quicksort([5,2,4,3]));
}
