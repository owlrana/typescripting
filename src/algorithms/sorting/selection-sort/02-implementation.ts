/**
 * Implementation of Selection Sort.
 */
export function selectionsort(arr: number[]): number[] {
    const a = [...arr];
    // TODO implement selection-sort
    return a;
}

if (require.main === module) {
    console.log(selectionsort([5,2,4,3]));
}
