/**
 * Implementation of Insertion Sort.
 */
export function insertionsort(arr: number[]): number[] {
    const a = [...arr];
    // TODO implement insertion-sort
    return a;
}

if (require.main === module) {
    console.log(insertionsort([5,2,4,3]));
}
